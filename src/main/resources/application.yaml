spring:
  application:
    name: comprameyguer
  datasource:
    url: jdbc:mysql://localhost:3306/comprameyguer
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      platform: mysql